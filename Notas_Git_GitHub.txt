git config --global user.email "your email": configuración necesaria.
git init: crea un repositorio vacío.
git add "path fichero": mete en un apartado intermedio el fichero, preparado para commit.
git add .: mete en el estado intermedio preparado para commit todos los cambios realizados en el repositorio.
git status: lista los archivos que están en el estado intemedio preparados para realizar el commit.
git commit -m "mensaje": Realiza un commit con los archivos en el estado intermdio con ese mensaje. Es identificado con id único.
git log: Muestra un historial con los commit realizados y datos sobre ellos.
git log --oneline: Muestra el historial de los commit, pero con menos información.
git remote add alias urlRemota: vincula mi repositorio local con mi repositorio remoto.
git remote -v: Lista los remotos.
git remote rename alias aliasNuevo: renombre el alias de un remoto asociado.
git remote rm alias: elimina el enlace entre el repositorio remoto y el local.
git push alias ramaEnLaQueEstas: Empuja mis cambios locales al remoto.
git clone urlRepositorio: Crea un repositorio local en el ordenador a partir del repositorio de la url. Ya quedan enlazados.

---------------------------------------------------------------------------------------------
Tres espacios:
Workspace: es el espacio de trabajo, donde estamos desarrollando.
Index: es la la zona intermedia para indexar los archivos. Se aÃ±aden con git add.
Repository local: es el repositorio local. Se aÃ±aden los archivos con git commit.
Repositorio Remoto: Es el repositorio en gitHub, bitbacked otro ordenador... se aÃ±aden los cambios del local con git push.

----------------------------------------------------------------------------------------------------
Importar proyecto ha eclipse
- Pulso botón derecho y voy a importar, eligo la opción de general o maven dependiendo el tipo de proyecto.
- existing projecto into workspace, eligo el proyecto del repositorio local y finish.
- Para sincronizar el espcaio de trabajo es botón derecho sobre el proyecto y sychronize workspace.
- Para realizar un commit, primero se añade el archivo al index mediante botón derecho add to index al archivo.
- Para hacer el commit se añaden los archivos del index que queremos hacer commit y se escribe el mensaje.
- Para hacer push botón derecho y push branch 'master'..., vemos el commit que vamos a subir y a donde
  y a que rama del remoto lo vamos a subir. Por último pulsamos finish.
